```javascript
function solution(num1, num2) {
    if(num1 === num2) { // 같으면 1
        return[1];
    } else {
        return[-1]; // 다르면 -1
    }
}

$(document).ready(function() {
    solution();
});
```