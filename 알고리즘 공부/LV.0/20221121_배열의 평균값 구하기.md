```javascript
function solution(numbers) {
    let answer = 0;

    // 1) 평균값 구하는 방법
    // 2) 배열의 값을 다 더한다.
    // 3) 다 더한 값을 더한 수의 값으로 나눈다.

    let sum = 0;

    for(i=0; i<numbers.length; i++) {
        sum += numbers[i];
    }

    answer = sum;

    return answer;
}
```