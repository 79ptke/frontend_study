```javascript
function solution(numbers) {
    let answer = [];
    //numbers = [1,2,3,4,5];
    numbers = [1,2,100,-99,1,2,3];

    for(i=0; i < numbers.length; i++) {
       answer.push(numbers[i]*2);
    }
   
    return answer;
}
```
